[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js plugin (auto-detected by Netlify, but explicit is better)
[[plugins]]
  package = "@netlify/plugin-nextjs"
